// Route shell only — the screen itself lives in src/features/payments so the
// feature's components stay together and can be reused (e.g. an edit screen).
export { default } from "@/src/features/payments/ReceivePaymentScreen";
