{
  "//": "Typecheck for the unit tests, which Node runs directly.",
  "//1": "They import with an explicit .ts extension because Node's test runner resolves real files;",
  "//2": "the app build (Metro) never sees them, so that stays out of the app config.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "allowImportingTsExtensions": true,
    "types": ["node"]
  },
  "include": ["src/**/*.test.ts"],
  "exclude": []
}
